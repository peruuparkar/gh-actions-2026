name: 02 - Building Blocks
run-name: ${{ github.actor }} is learning GitHub Actions
on: 
  push:
  workflow_dispatch:
jobs:
  check-bats-version:
    runs-on: self-hosted
    steps:
      - run: echo hello
      - run: echo hello-2
